{
  "name": "production-master",
  "version": "0.1.1",
  "description": "pmctl — operator CLI for Production Master over the same BFF as every IDE surface. Reuses @production-master/plugin-core (ServiceClient, device-code auth, token store, SSE event stream). Imports NO LLM/provider SDK. Published unscoped so `npx production-master` works from a clean machine; the binary is still called pmctl.",
  "type": "module",
  "bin": {
    "pmctl": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@production-master/plugin-core": "0.1.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ProductionMasterAI/production-master.git",
    "directory": "packages/pmctl"
  },
  "homepage": "https://github.com/ProductionMasterAI/production-master#readme",
  "bugs": "https://github.com/ProductionMasterAI/production-master/issues",
  "license": "MIT"
}
